// 030_secondMax.cpp : 이 파일에는 'main' 함수가 포함됩니다. 거기서 프로그램 실행이 시작되고 종료됩니다.
//

#include <stdio.h>

int main()
{
    int a[10];
    int max, sec;

    printf("10개의 숫자 입력: ");
    for (int i = 0; i < 10; i++)
        scanf_s("%d", &a[i]);

    
// max와 second max 를 찾아라




    printf("i=%d = %d , sec = %d\n",i, max, sec);
}

